<!DOCTYPE html>
<html>
  <head>
    <title>ToyApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>

  <body class="d-flex flex-column min-vh-100">
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container d-flex justify-content-between">
        <a class="navbar-brand" href="/">ToyApp</a>
        <div class="">
          <div class="navbar-nav">
            <%= link_to "Users", users_path, class: current_controller?('users') ? 'text-success' : '' %>
            <span class="mx-2"></span>
            <%= link_to "Microposts", microposts_path, class: current_controller?('microposts') ? 'text-success' : '' %>
          </div>
        </div>
      </div>
    </nav>

    <div style="width: 800px; margin: 50px auto;">
      <%= yield %>
    </div>

    <footer class="mt-auto bg-primary p-2 text-center text-white">
      Copyright&copy; 2024
    </footer>
  </body>
</html>
